instalar postgres;
generar una base de datos lynxspace;
lo mismo que hicimos en mysql;

una nueva terminal
root;
su postgres
psql
create database lynxspace;
create user master with password'1234';
grant all privileges on database lynxpace to master;
\q (para salirnos)
exit

nano /etc/postgresql/11/main/tabulador
archivo se llama pg_hba.conf
despues de peer
poner local all md5 guardamos ctrl o enter control x
service postgresql restart
psql -u master -d lynsxpace
create table usuario (id_usuario serial primary key, email varchar (100), contrasena varchar (32));
alter table usuario add constraint email_uq unique (email);
create table persona (id_persona serial primary key, nombre varchar (50), apellidos varchar(100), apodo varchar(50), id_usuario int references usuario (id_usuario), nacimiento date);

control alt f5
apt install php-pgsql
service apache2 restart
control alt f3
select * from asuario;
